meta {
  name: Get Recent
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/workouts?limit=10
  body: none
  auth: bearer
}

params:query {
  limit: 10
}

auth:bearer {
  token: {{accessToken}}
}

docs {
  Most recent workouts first (what the home dashboard shows). `limit` is optional.
}

import { Component } from '@angular/core';

/** Placeholder — the reset-password page (reached from the email link) is built in M6. */
@Component({
  selector: 'app-reset-password',
  imports: [],
  template: `<h1 class="page-title">Reset password</h1>`,
})
export class ResetPassword {}

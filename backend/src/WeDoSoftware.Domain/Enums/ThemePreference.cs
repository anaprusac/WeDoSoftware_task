namespace WeDoSoftware.Domain.Enums;

/// <summary>
/// The user's preferred UI theme, persisted so the choice follows the account.
/// </summary>
public enum ThemePreference
{
    Light = 0,
    Dark = 1
}
